FoamFile { version 2.0; format ascii; class volVectorField; object U; }
dimensions [0 1 -1 0 0 0 0];
internalField setExpr
{
    expr "vector(sin(pos().x)*cos(pos().y), -cos(pos().x)*sin(pos().y), 0)";
};
boundaryField { periodic { type cyclic; } }
