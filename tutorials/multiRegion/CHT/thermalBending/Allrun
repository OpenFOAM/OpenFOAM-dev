#!/bin/sh
cd ${0%/*} || exit 1    # Run from this directory

# Source tutorial run functions
. $WM_PROJECT_DIR/bin/tools/RunFunctions

runApplication -suffix plate blockMesh -region plate
runApplication -suffix plate createPatch -region plate

runApplication -suffix heatsink blockMesh -region heatsink
runApplication -suffix heatsink createPatch -region heatsink

runApplication foamMultiRun

#------------------------------------------------------------------------------
