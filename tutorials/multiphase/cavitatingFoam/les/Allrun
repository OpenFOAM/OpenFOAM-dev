#!/bin/sh
cd ${0%/*} || exit 1    # run from this directory

(
    cd throttle || exit
    ./Allrun
)

(
    cd throttle3D || exit
    ./Allrun
)

# ----------------------------------------------------------------- end-of-file
