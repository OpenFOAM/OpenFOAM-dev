#!/bin/sh
cd ${0%/*} || exit 1    # Run from this directory

(
    cd throttle || exit
    ./Allrun $*
)

(
    cd throttle3D || exit
    ./Allrun $*
)

#------------------------------------------------------------------------------
